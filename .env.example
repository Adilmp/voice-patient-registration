# SQLite by default; swap for a Postgres URL in production without code changes.
DATABASE_URL=sqlite:///./patients.db

# Insert the two demo patients on first boot if the table is empty.
SEED_DATA=true

PORT=8000

# --- Voice agent (Vapi) — filled in during the telephony integration step ---
# VAPI_API_KEY=
# VAPI_PHONE_NUMBER_ID=
